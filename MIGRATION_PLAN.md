# LETUS LOGIS — AI Playground 전환 준비

> Vercel 호스팅 만료(2026년 6월 말) 대응 계획  
> 작성일: 2026-05-28  
> 참고: AI추진팀 공지 (AI Playground 안내)

---

## 한 줄 요약

**"자체 서버 세팅 없음. 사내 Git 이전 → AI Playground 등록 신청 → 심사 통과 → 끝"**

Nginx, IIS 같은 서버 설정을 직접 할 필요가 없습니다.  
AI Playground가 호스팅 인프라를 제공합니다.

---

## 현재 상태 vs 목표 상태

| 항목 | 현재 | 목표 |
|------|------|------|
| 코드 저장소 | GitHub | **사내 Git** (이전 필수) |
| 프론트엔드 호스팅 | Vercel | **AI Playground** |
| 백엔드 / DB | Supabase Cloud | **미결정** (아래 참고) |
| RPA Worker | 사내 PC (Node.js 데몬) | 그대로 유지 |
| 내부 DB | MS-SQL 사내망 | 그대로 유지 |

---

## AI Playground 등록 절차 (공식)

AI추진팀 공지 기준 4단계:

```
1단계. 소스코드를 사내 Git으로 이전
         ↓
2단계. 앱 등록 신청서 제출
       (앱 이름 / 기능 설명 / 사내 Git 주소 / 대상 브랜드)
         ↓
3단계. AI추진팀 보안 검토 (통상 3영업일)
         ↓
4단계. 검토 완료 → AI Playground 등록 및 공개
```

---

## 작업 ①: GitHub → 사내 Git 이전 (지금 시작 가능)

사내 Git 계정/프로젝트가 생기면 바로 진행할 수 있습니다.

### 사전 조건

```
□ 사내 Git 서버 URL 확보  (AI Hub > Library 가이드 확인)
□ 사내 Git 계정 생성
□ 새 프로젝트(저장소) 생성
□ .env 파일 보관 위치 결정 (사내 Git에도 절대 올리면 안 됨)
```

### 이전 명령어

```bash
# 1. GitHub 레포를 미러 클론 (브랜치·태그 전부 포함)
git clone --mirror https://github.com/[계정]/[레포명].git temp-mirror
cd temp-mirror

# 2. 사내 Git으로 push
git push --mirror http://[사내Git주소]/[그룹]/[레포명].git

# 3. 로컬 작업 폴더의 remote 주소 변경
cd ../LetusLogis
git remote set-url origin http://[사내Git주소]/[그룹]/[레포명].git
git remote -v   # 확인

# 4. 임시 폴더 삭제
cd ..
rm -rf temp-mirror
```

### 팀원 공지용 (이전 후 전달)

```bash
# 팀원들도 아래 명령어 한 줄 실행하면 됩니다
git remote set-url origin http://[사내Git주소]/[그룹]/[레포명].git
```

---

## 작업 ②: 등록 신청서 작성 준비

AI추진팀에 제출할 내용을 미리 정리해 두세요.

| 항목 | 내용 |
|------|------|
| 앱 이름 | LETUS LOGIS |
| 기능 설명 | 일룸 물류 입고 특이사항 통합 관리 시스템. 입고 특이사항 등록·관리·피드백, WMS D-2 결품 조회, 모바일 PWA 지원 |
| 사내 Git 주소 | (이전 완료 후 기재) |
| 대상 브랜드 | 일룸, 시디즈, 데스커 등 퍼시스 계열 브랜드 |
| 사용자 범위 | 물류팀 내부 직원 + 현장 작업자 |

---

## ⚠️ 반드시 확인해야 할 사항 3가지

### 확인 ①: 🚨 외부(현장) 접속 가능한가? — 가장 중요

이 앱은 현장 작업자가 **회사 밖에서 스마트폰으로** 접속합니다.

```
현재:  현장 작업자 (외부 IP) → Vercel (외부 서버) → 접속 ✅
이후:  현장 작업자 (외부 IP) → AI Playground (사내망) → 접속 ❌
```

EP 계정은 인가된 IP(사내망)에서만 접속 가능하기 때문에,  
AI Playground로 이전하면 **현장 PWA 기능이 통째로 차단됩니다.**

영향받는 기능:
- 모바일 바코드 스캔 + 특이사항 등록
- 현장 작업자의 조치결과 사진 업로드
- 회수품 등록 / 조회

> **AI추진팀에 물어볼 것:**  
> "이 앱은 외부망(현장 작업자 스마트폰)에서 접속하는 기능이 필수입니다.  
>  AI Playground에서 외부 접근을 허용하는 방법이 있나요?"

| 답변 | 대응 |
|------|------|
| 외부 접근 허용 가능 | 구조 변경 없이 진행 |
| VPN 사용 권장 | 현장 폰마다 VPN 설정 필요 (IT팀 협의) |
| 불가 — 분리 배포 필요 | 데스크톱/모바일 따로 배포 (추가 공수) |
| 불가 — 방법 없음 | 모바일 접속 방식 전면 재검토 필요 |

---

### 확인 ②: AI Playground 서버에서 Supabase Cloud 연결 가능한가?

이 앱은 Supabase Cloud(supabase.co)에 연결해서 동작합니다.  
AI Playground 서버가 외부 인터넷 아웃바운드를 허용하는지 확인이 필요합니다.

> **AI추진팀에 물어볼 것:**  
> "AI Playground에 등록된 앱이 외부 API(supabase.co, HTTPS 443)를 호출할 수 있나요?"

| 결과 | 대응 |
|------|------|
| **가능** | Supabase 그대로 유지, 추가 작업 없음 |
| **불가** | Supabase 자체 호스팅 필요 (공수 큼, 별도 협의) |

### 확인 ③: EP 계정 인증 vs 기존 Supabase Auth

공지에 "EP계정을 통한 자동 사용자 인증 지원"이라고 나와 있습니다.  
현재 이 앱은 Supabase Auth로 자체 로그인을 구현하고 있습니다.

> **AI추진팀에 물어볼 것:**  
> "EP 계정 인증이 강제인가요, 아니면 앱 자체 인증(Supabase Auth)도 사용 가능한가요?"

| 결과 | 대응 |
|------|------|
| **앱 자체 인증 허용** | 현재 로그인 방식 그대로 유지 |
| **EP 인증 강제** | LoginView.jsx + useAuth.jsx 수정 필요 (EP SSO 연동) |

---

## 환경변수 처리

Vercel에서는 Vercel 대시보드에서 관리하던 환경변수를,  
AI Playground 등록 시 어떻게 설정하는지 확인이 필요합니다.

> **AI추진팀 또는 AI Hub 가이드에서 확인할 것:**  
> "앱 등록 시 환경변수(API Key 등)는 어떻게 설정하나요?"

현재 사용 중인 환경변수:

```
VITE_SUPABASE_URL
VITE_SUPABASE_ANON_KEY
VITE_SUPABASE_SERVICE_ROLE_KEY
```

> `.env` 파일은 사내 Git에 올리면 절대 안 됩니다.  
> AI Playground가 환경변수 설정 UI를 제공하거나, 빌드 시 주입 방법을 안내할 것입니다.

---

## 전체 체크리스트

```
[즉시 가능]
□ AI Hub > Library 에서 사내 Git 이전 가이드 확인
□ 사내 Git 계정 및 프로젝트 생성 신청
□ GitHub → 사내 Git 미러 이전
□ 팀원 remote URL 변경 공지

[AI추진팀 확인 필요] ← 이거 먼저
□ 🚨 외부망(현장 작업자 스마트폰)에서 접속 가능한가?
□ AI Playground 서버에서 Supabase Cloud(외부 HTTPS) 호출 가능 여부
□ EP 계정 인증 강제 여부 (앱 자체 인증 허용되는지)
□ 환경변수(API Key 등) 설정 방법

[확인 후 진행]
□ 앱 등록 신청서 작성 및 제출
□ 보안 검토 대기 (3영업일)
□ 등록 완료 후 기존 Vercel URL로 공유된 링크 교체
□ 기존 Vercel 프로젝트 삭제
```

---

## 6월 말 전 권장 타임라인

| 시기 | 작업 |
|------|------|
| **지금 ~ 6/7** | AI Hub 가이드 확인, 사내 Git 계정 신청 |
| **6/7 ~ 6/14** | GitHub → 사내 Git 이전, AI추진팀 2가지 확인 |
| **6/14 ~ 6/18** | 앱 등록 신청서 제출 |
| **6/18 ~ 6/23** | 보안 검토 대기 (3영업일) |
| **6/23 ~ 6/28** | 등록 완료, 링크 교체, Vercel 종료 |

> 보안 검토가 3영업일인데, 주말·공휴일 제외하면 실제로 일주일 잡는 게 안전합니다.  
> **6월 14일 전에 신청서 제출하는 것을 권장합니다.**
