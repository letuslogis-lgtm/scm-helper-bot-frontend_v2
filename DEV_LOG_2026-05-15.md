# 개발 내역 — 2026-05-15

## 개요

입고 특이사항 플로우 전반을 개편하고, 작업자가 조치 결과를 관리자에게 역방향으로 전달하는 기능을 추가했다.

---

## 1. 작업자 조치 결과 전달 기능 (양방향 피드백)

### 배경
기존 플로우는 `작업자 등록 → 관리자 조치 → 현장 피드백`으로 단방향이었다.  
관리자가 "반송 처리" 등의 피드백을 보낸 후, 작업자가 실제로 처리한 결과(사진·메모)를 다시 관리자에게 전달할 수단이 없었다.

### 구현

**모바일 (`MobileMyIssues.jsx`)**
- `WorkerResponseSheet` 컴포넌트 신규 추가
  - 조치 결과 메모(텍스트) + 사진 최대 3장 업로드
  - 이미지 압축: canvas → JPEG 0.6 → Supabase `issue_images` 버킷 업로드
  - 여러 장 URL은 쉼표 구분 문자열로 저장 (기존 `image_url` 패턴 동일)
- `IssueDetailSheet`에 "조치 결과 전달하기" 버튼 추가
  - **노출 조건**: `status === '조치완료'` AND `is_notified === true` (피드백 전송 후에만)
  - 이미 전달한 경우 결과(텍스트+사진)를 읽기 전용으로 표시

**DB 컬럼 추가 (Supabase SQL)**
```sql
ALTER TABLE logistics_issues
  ADD COLUMN worker_response        text,
  ADD COLUMN worker_response_photos text,
  ADD COLUMN worker_responded_at    timestamptz;
```
> 이미 이전 세션에서 추가됨. 재실행 시 에러 발생하므로 주의.

**스토리지 정책**
- 버킷: `issue_images`
- 경로: `worker-response/{timestamp}_{random}.jpg`
- 기존 RLS 정책(`worker_response_upload`) 이미 적용됨

---

## 2. 입고 특이사항 플로우 구조 개편

### 변경 전후 비교

| 구분 | 변경 전 | 변경 후 |
|---|---|---|
| 플로우 | 접수 → 이관 → 구매/생산 확인 및 조치 → 피드백 | 접수 → **이관 or 직접조치** → 구매/생산 확인 → 담당자 조치 → 피드백(자동) |
| 피드백 전송 | 수동 (IssueList "피드백 전송" 버튼) | 조치완료 시 자동 (`is_notified`, `feedback_sent_at` 자동 설정) |
| 조치 내용 저장 | 버그: `request_content`에 덮어쓰기 | 수정: `action_content`에 저장 |

### 세부 변경 사항

#### RequestModal 개편 (`SupportCenter.jsx`)
- 3단계 진행 표시바 추가: ① 접수 › ② 이관 › ③ 구매/생산 확인 › ④ 담당자 조치 › ⑤ 피드백
- 각 단계 상태에 따라 active(주황) / done(초록) / pending(회색) 스타일 분기
- `조치대기` 상태에서 버튼 2개 제공
  - **직접 조치** (초록): 구매/생산 확인 단계 건너뛰고 HandleModal 바로 오픈
  - **② 이관** (노랑): 기존과 동일, 구매/생산팀 확인 요청 단계 진행
- `처리 중` + 구매/생산 확인 미완료 상태: 확인 내용 입력 후 "③ 구매/생산 확인 완료" 버튼
- `purchase_response` 컬럼 유무로 단계 완료 여부 판단 (status 추가 없이 처리)

**DB 컬럼 추가 (Supabase SQL)**
```sql
ALTER TABLE logistics_issues ADD COLUMN purchase_response text;
```

#### HandleModal 개편 (`SupportCenter.jsx`)
- 버그 수정: 조치 내용 입력 후 저장 시 `request_content`에 덮어쓰던 문제 수정 → `action_content`에 정상 저장
- 우측 패널 레이아웃: ① 접수내용(읽기전용) → ③ 구매/생산 확인내용(있을 경우, 읽기전용) → ④ 담당자 조치(입력)
- 조치완료 버튼: `is_notified: true`, `feedback_sent_at` 자동 설정하여 피드백 자동 발송
- 버튼 레이블: "④ 조치완료 (피드백 자동 전송)"

#### IssueList 연동 (`IssueList.jsx`)
- `RequestModal`에 `onDirectHandle` 콜백 추가
  - "직접 조치" 클릭 시 RequestModal을 닫고 HandleModal을 바로 오픈

---

## 3. 바코드 스캔 색상 수정 (`MobileIssueRegister.jsx`)

### 문제
`analyze-barcode` Edge Function이 `단품코드`만 반환하는 경우, 색상 suffix 없이 저장되는 문제.  
회수품 처리 로직 추가 후 입고 특이사항 쪽에서 색상 조회가 누락됨.

### 수정
바코드 분석 결과에 하이픈(`-`)이 없으면 `products` 테이블에서 `item_color`를 조회해 `단품코드-색상` 형태로 조합.

```javascript
if (data?.product_code) {
    let fullCode = data.product_code;
    if (!fullCode.includes('-')) {
        const { data: pd } = await supabase
            .from('products')
            .select('item_color')
            .eq('item_code', fullCode)
            .single();
        if (pd?.item_color) fullCode = `${fullCode}-${pd.item_color}`;
    }
    setProductCode(fullCode);
}
```

---

## 4. 모바일 시트 깨짐 수정 (`MobileMyIssues.jsx`)

### 문제
`WorkerResponseSheet`에서 사진 촬영 후 앱으로 돌아올 때 화면 레이아웃이 깨지는 현상.

### 원인
- 카메라 앱 복귀 시 body 스크롤이 풀리며 배경 페이지가 이동
- `85vh` 기준이 모바일 주소창/하단바 등장 여부에 따라 달라져 시트 높이 흔들림

### 수정
- **body scroll lock**: 시트 마운트 시 `document.body.style.overflow = 'hidden'`, 언마운트 시 복원
- **`vh` → `svh`** (small viewport height): 브라우저 UI와 무관하게 안정적인 높이 기준 사용
- **`overscroll-contain`**: 시트 내부 스크롤이 끝나도 배경으로 전파되지 않도록 차단
- `IssueDetailSheet`에도 동일하게 `svh`, `overscroll-contain` 적용

---

## 수정 파일 목록

| 파일 | 변경 내용 |
|---|---|
| `src/MobileMyIssues.jsx` | WorkerResponseSheet 신규, IssueDetailSheet 조치결과 섹션 추가, 모바일 시트 깨짐 수정 |
| `src/MobileIssueRegister.jsx` | 바코드 색상 조회 로직 추가 |
| `src/SupportCenter.jsx` | RequestModal 3단계 개편 + 직접조치 버튼, HandleModal 전면 재작성 |
| `src/IssueList.jsx` | RequestModal onDirectHandle 콜백 연결 |

---

## 미결 항목

- [ ] Supabase SQL 실행 확인
  ```sql
  ALTER TABLE logistics_issues ADD COLUMN purchase_response text;
  ```
- [ ] 전체 플로우 통합 테스트: 이관 → 구매/생산 확인 → 담당자 조치 → 자동 피드백 → 모바일 조치 결과 전달
- [ ] 직접 조치 경로 테스트: 접수 → 직접 조치 → HandleModal 즉시 오픈
